# Files

- [Service Layer — Orchestration, Search & Trust-Block Assembly](business-logic.md) - How the DirectoryService orchestrates HAAP directory business logic — L1 proof-of-endpoint registration and signed heartbeats, SearchQuery parsing and in-memory filtering, wiring of the L2–L5 sub-services, and verdict-free trust-block assembly with explicit "no signal" defaults.
- [HTTP Layer — Routing, Errors, Rate Limits & Observability Endpoints](http-layer.md) - The full HTTP surface of the HAAP directory — the stdlib ThreadingHTTPServer in http_api.py with its /v1 route table and legacy aliases, request parsing and body caps, the stable {"error":{code,message,request_id}} envelope, per-IP token-bucket rate limiting with loopback-only proxy-header trust, signed audit responses, and the /health and /metrics endpoints.
- [System Overview — Topology, Trust Ladder & Build Phases](overview.md) - Big-picture architecture of the HAAP Public Directory — process entry points, the HTTP → DirectoryService sub-services → Store layering, the L0–L5 trust ladder mapped to code modules, the F0–F6 build status, and the constraints that shape every change.
- [Store & Audit Chain — SQLite Schema, Transactions and the Audit-Equals-Mutation Invariant](store-and-audit.md) - How the SQLite persistence layer works — every table of the schema, the single-connection RLock-serialized BEGIN IMMEDIATE write model, and the L5 hash-chained audit log whose core rule is that every state-changing write appends exactly one chain entry inside the same transaction.
