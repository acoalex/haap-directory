# Files

- [Federation & Mirrors — Consuming the Audit Chain Externally](federation-mirror.md) - The consumer/operator view of federation — how an independent mirror ingests /v1/audit/log via haap_directory.mirror.ingest_chain, verifies chain linkage locally with audit.verify_chain and reproduces an identical head, what two agreeing mirrors prove, and the consumer-side trust actions (poll the head, re-verify the agent's own /.well-known/haap.json) that make single-directory v1 federation safe.
- [Legacy haap Client Compatibility — Wire Contract](legacy-client.md) - The wire-compatibility contract between this directory and the unmodified haap client package (haap/registry_client.py) — which legacy routes and body shapes must keep working (POST /register, /register/complete, unsigned /heartbeat, GET /search and /agents/{fp} bare-manifest projections), how v1 and legacy differ only in serialization and status code, why canonical JSON is vendored byte-identical rather than imported, and the test contract that proves it by running the real client from HAAP_REPO or a sibling ../haap checkout.
