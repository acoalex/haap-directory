# Files

- [Audit Transparency (L5) — Chain Endpoints, Checkpoints & Signed Responses](audit-transparency.md) - The external face of the HAAP directory's L5 append-only hash-chained audit log — the /v1/audit/head|log|checkpoints|verify and /v1/agents/{fp}/audit endpoints, hourly plus shutdown checkpoint signing, X-HAAP-Directory-Signature response signing, the tamper-evidence property with its polling duty, and how the chain is the ingest seam for mirrors.
- [Domain Verification (L2) — Server-Side Domain-Control Checks](domain-verification.md) - The L2 domain-verification protocol of the HAAP directory — POST /v1/verify-domain and /v1/verify-domain/confirm with signed subsets and single-use 30-minute tokens, the two server-executed checks (dig-based _haap TXT lookup and the 4 KiB/10 s/10 s TLS well-known fetch with same-registrable-domain redirects), the endpoint-match rule, 90-day validity with time-filtered downgrade, the domain_verified trust-block signal that is never KYC, and the injectable Resolver seam both production and tests share.
- [Moderation & Appeals (L4) — Moderator-Keyed Human Actions](moderation-appeals.md) - The L4 human-in-the-loop layer of the HAAP directory — operator-held moderator Ed25519 keys configured via moderator_keys, signed takedown/suspend/unsuspend actions over exact payload subsets (MODERATOR_UNKNOWN and TAKEDOWN_UNAUTHORIZED 403 gates), agent-signed appeals into the appeals table with off-band decisions, suspension state (status suspended plus suspension_json rule/evidence_reports/at) with fresh-TTL restoration on unsuspend, L5 audit attribution with actor moderator:<fp> or directory, and docs/MODERATION.md as the append-only runbook and policy log.
- [Registration & Heartbeat Lifecycle (L1 Proof-of-Endpoint)](registration-lifecycle.md) - The L1 registration lifecycle of the HAAP directory — two-step submit/complete proof-of-endpoint with a single-use 120 s nonce challenge bound to the validated public key, the write-lock replay guard, signed (v1) and unsigned (legacy) heartbeats, TTL expiry with startup and lazy pruning, update-vs-fresh re-registration that preserves registered_at, capacity caps, and the stable error codes guarding each state.
- [Reputation (L4) — Signed Reports, Decay & Auto-Suspend](reputation.md) - The L4 behavioural-reputation layer of the HAAP directory — signed report intake over strict whitelists (categories, severities, evidence kinds), reporter tenure gating (72 h) and duplicate windows (24 h) that decide the stored counts flag, sha256 evidence/description hashing with raw bodies kept off-chain per docs/MODERATION.md, decayed counters (180-day window) surfaced in the trust block and GET /v1/agents/{fp}/reports, and the deterministic auto-suspend automaton (≥3 unique eligible reporters in an abuse class within a rolling 7-day window) recorded as suspend_agent with actor directory and event report.auto_suspend, plus block_recommendation and report_war annotations.
- [Vouching (L3) — Signed Peer Statements & the Trust Graph](vouching.md) - The L3 vouching layer of the HAAP directory — POST /v1/vouches grants and DELETE /v1/vouches/{id} revokes signed, scoped, expiring peer statements behind live-party gates (VOUCHER_NOT_LISTED / VOUCHEE_NOT_LISTED), a weight-forced-to-1 field rule, a 180-day expiry window, a 10-edge outgoing cap (VOUCH_LIMIT_REACHED) and duplicate-scope rejection (VOUCH_EXISTS), with signatures verified over the body minus signature; the read surface serves raw inbound and outgoing edge lists with per-edge voucher_tenure_hours plus structural annotations (mutual_vouch_density, honest no-signal vouchers_share_registration_cluster), depth-clamped-to-2 BFS trust paths over active edges only, and per-edge vouches_in in the trust block — never a score.
