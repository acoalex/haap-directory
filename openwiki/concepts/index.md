# Files

- [Signed Wire Format — Canonical JSON, Ed25519 Conventions & Frozen Contracts](signed-wire-format.md) - The cross-cutting substrate every HAAP directory signature depends on — the frozen byte-for-byte canonical-JSON serialization, Ed25519 raw-key and base64 conventions, fingerprint derivation and recompute rule, the key-as-credential request-signing pattern (verify_over/subset) with the exact signed bytes per endpoint, manifest hard-validation rules, and the add-only stable error-code contract.
- [Trust Model — The L0–L5 Ladder and "Phone Book, Not Notary"](trust-model.md) - The semantic backbone of the HAAP Public Directory — why the directory is a phone book, not a notary and never a judge; the L0 crypto identity through L5 audit-transparency ladder with what each layer proves and explicitly does not, who decides at every layer, how an attacker bypasses it, Sybil economics, and how the model materializes in code as labelled trust-block signals with provenance that the consumer weighs.
